# TH模型学习阶段历史最大值与连接更新公式

本文档用纯文本和数学公式说明模型在学习阶段如何记录神经活动历史、如何从历史中计算带衰减的最大活动，以及这些历史最大值如何用于兴奋性和抑制性连接的权重更新。

这里按上游细胞类型定义连接性质：上游为兴奋性细胞的连接称为兴奋性连接，上游为抑制性细胞的连接称为抑制性连接。权重矩阵统一约定为：行表示下游细胞，列表示上游细胞。

## 1. 神经群体与活动历史

模型包含三个兴奋性群体：浅层兴奋性群体、第五层奖励接收群体、第五层读出群体。一次决策过程第 $t$ 个迭代步的兴奋性总活动记为

$$
x_t=
\begin{bmatrix}
x_t^{23}\\
x_t^{5R}\\
x_t^{5D}
\end{bmatrix}.
$$

其中 $x_t^{23}$ 表示浅层兴奋性群体活动，$x_t^{5R}$ 表示第五层奖励接收群体活动，$x_t^{5D}$ 表示第五层读出群体活动。

每个兴奋性群体都对应一个局部抑制池。三个抑制池在第 $t$ 个迭代步的活动分别记为

$$
h_t^{23},\qquad h_t^{5R},\qquad h_t^{5D}.
$$

一次学习事件发生前，模型会保存从初始状态到学习事件触发时刻的活动历史。兴奋性历史矩阵为

$$
X=
\begin{bmatrix}
x_1 & x_2 & \cdots & x_T
\end{bmatrix},
$$

三个抑制性历史矩阵为

$$
H^{23}=
\begin{bmatrix}
h_1^{23} & h_2^{23} & \cdots & h_T^{23}
\end{bmatrix},
$$

$$
H^{5R}=
\begin{bmatrix}
h_1^{5R} & h_2^{5R} & \cdots & h_T^{5R}
\end{bmatrix},
$$

$$
H^{5D}=
\begin{bmatrix}
h_1^{5D} & h_2^{5D} & \cdots & h_T^{5D}
\end{bmatrix}.
$$

如果某个试次在中途已经达到反应阈值，学习事件在首次达到阈值时触发，历史只记录到该时刻为止。如果整个决策过程都未达到阈值，则学习事件在最后一个迭代步后触发，历史包含完整决策过程。

在当前主线流程中，正式线索学习之前会先经过一次非目标线索状态筛选。该非目标状态会作为正式线索阶段的初始状态，因此正式线索学习阶段的历史最大值可以包含这一初始状态遗留的活动成分。

## 2. 带衰减的历史最大值

令 $\lambda$ 为历史衰减因子，且 $0<\lambda\le 1$。对任意活动历史矩阵 $Z\in\mathbb{R}^{n\times T}$，第 $i$ 个细胞的带衰减历史最大值定义为

$$
m_i(Z)=\max_{1\le t\le T}\left(\lambda^{T-t}Z_{i,t}\right).
$$



$$
\begin{flalign*} &u &= \max_{1\le t\le T}\left(\lambda^{T-t}a_t\right)\\ &\Delta W^E &= \eta\left(u_{\mathrm{post}}-\theta\right)u^E_{\mathrm{pre}}\\ &\Delta W^I &= \eta u_{\mathrm{post}}\left(u^I_{\mathrm{pre}}-u_{\mathrm{post}}\right) \end{flalign*}
$$


最新一列的权重为 $1$，倒数第二列的权重为 $\lambda$，更早的列依次为 $\lambda^2,\lambda^3,\ldots$。因此，历史最大值不是简单地取整个历史的最大活动，而是先让较早活动按时间距离衰减，再逐细胞取最大值。

把该运算写成向量形式：

$$
m(Z)=
\begin{bmatrix}
m_1(Z)\\
m_2(Z)\\
\vdots\\
m_n(Z)
\end{bmatrix}.
$$

这一向量就是学习更新时使用的历史最大活动。

## 3. 上游历史最大值与下游历史最大值

在有教学信号的正式线索学习事件中，模型区分上游活动历史和下游活动历史。

上游历史最大值直接来自教学发生前的真实决策历史：

$$
a^{\mathrm{up}}=m(X).
$$

下游历史最大值来自教学修正后的历史。令 $s$ 为教学信号强度，令 $q^{5D}$ 为第五层读出群体的目标活动模板，则最后一列读出群体活动被修正为

$$
\widetilde{x}_T^{5D}=(1-s)x_T^{5D}+s q^{5D}.
$$

浅层群体和第五层奖励接收群体不被该教学信号直接改写：

$$
\widetilde{x}_t^{23}=x_t^{23},
\qquad
\widetilde{x}_t^{5R}=x_t^{5R}.
$$

第五层读出群体在最后一列之外的历史也保持不变：

$$
\widetilde{x}_t^{5D}=x_t^{5D},\qquad t<T.
$$

由此得到教学修正后的兴奋性历史矩阵 $\widetilde{X}$，下游历史最大值为

$$
a^{\mathrm{down}}=m(\widetilde{X}).
$$

将其按三个兴奋性群体拆分：

$$
a^{\mathrm{down}}=
\begin{bmatrix}
e^{23}\\
e^{5R}\\
e^{5D}
\end{bmatrix}.
$$

其中 $e^{23}$、$e^{5R}$、$e^{5D}$ 分别是三个兴奋性群体在学习更新中作为下游活动使用的历史最大值。

如果没有教学信号，或在非目标线索反训练过程中，则上游和下游使用同一个历史最大值：

$$
a^{\mathrm{up}}=a^{\mathrm{down}}=m(X).
$$

## 4. 抑制池历史最大值

抑制池也使用同样的衰减最大值运算。浅层抑制池和第五层奖励接收抑制池的学习活动为

$$
b^{23}=m(H^{23}),
$$

$$
b^{5R}=m(H^{5R}).
$$

在有正教学信号时，第五层读出抑制池最后一列会被替换为教学指定的抑制性目标活动。令 $q_I^{5D}$ 为第五层读出抑制池的目标活动模板，则

$$
\widetilde{h}_T^{5D}=s q_I^{5D},
\qquad
b^{5D}=m(\widetilde{H}^{5D}).
$$

若没有正教学信号，则直接使用原始抑制池历史：

$$
b^{5D}=m(H^{5D}).
$$

在进入连接更新前，活动统一取非负部分：

$$
[z]_+=\max(z,0).
$$

## 5. 兴奋性上游连接更新

本节统一讨论“上游是兴奋性细胞”的连接更新，不再按下游所在区域单独拆分。不同下游区域只改变取到的活动向量，不改变规则组织方式。

令 $u^{E}_{\mathrm{pre}}$ 表示兴奋性上游细胞学习活动，$u_{\mathrm{post}}$ 表示下游目标细胞学习活动，则不管下游是兴奋性还是抑制性群体，兴奋性上游连接都用同一个阈值参数 $\theta_E$，统一写为

$$
\Delta W^{E\to *}_{ij}
=\eta\left(u_{\mathrm{post},i}-\theta_E\right)u^{E}_{\mathrm{pre},j}.
$$

其中 $u_{\mathrm{post}}$ 由下游类型决定（例如可取兴奋性活动向量或抑制池活动向量），但减去的阈值参数始终是同一个 $\theta_E$。更新后统一做上界裁剪：

$$
W^{E\to *}\leftarrow\min\left(W^{E\to *}+\Delta W^{E\to *},\,W_{\max}\right).
$$

若目标连接是兴奋性群体内部连接，还需额外清零自连接：

$$
W^{E\to E}_{ii}\leftarrow0.
$$

## 6. 抑制性上游连接更新

本节统一讨论“上游是抑制性细胞”的连接更新。无论下游是浅层群体、第五层奖励接收群体还是第五层读出群体，都按同一类规则组织。

令 $u_{\mathrm{post}}$ 表示下游目标细胞学习活动，$u_{\mathrm{pre}}$ 表示抑制性上游细胞学习活动，则抑制性上游连接可统一写成

$$
\Delta W^{I\to *}=\eta\,u_{\mathrm{post}}\left(u_{\mathrm{pre}}-u_{\mathrm{post}}\right).
$$

更新后统一做上界裁剪：

$$
W^{I\to *}\leftarrow\min\left(W^{I\to *}+\Delta W^{I\to *},\,W_{\max}\right).
$$

其中抑制池内部自连接清零：

$$
W^{I\to I}_{jj}\leftarrow0.
$$

## 7. 统一解释

因此，文中的更新律不再按“读出区一套、跨区一套、其它区域一套”来分段描述，而是按上游类型统一归类：

$$
\quad \text{上游为兴奋性细胞} \Rightarrow \text{使用兴奋性上游连接更新规则}
$$

$$
\quad \text{上游为抑制性细胞} \Rightarrow \text{使用抑制性上游连接更新规则}
$$

下游区域只影响代入的活动向量，不改变规则的组织类别。

## 8. 正式教学更新与非目标反训练更新

正式线索学习事件使用教学修正后的下游历史最大值：

$$
a^{\mathrm{up}}=m(X),
\qquad
a^{\mathrm{down}}=m(\widetilde{X}).
$$

因此，正式教学更新把真实决策过程中的上游活动与教学信号指定的下游目标活动绑定起来。

非目标线索反训练不引入教学模板，它使用同一个历史最大值作为上游和下游：

$$
a^{\mathrm{up}}=a^{\mathrm{down}}=m(X).
$$

同时，非目标反训练使用负学习率：

$$
\eta_{\mathrm{anti}}=-\eta.
$$

因此，反训练中所有连接改变量仍服从上面的同一组规则，只是将 $\eta$ 替换为 $-\eta$。其作用是削弱那些会让非目标线索错误触发反应的路径。
