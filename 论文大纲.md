# 学位论文大纲（规范化结构草案）：迁移阶段 LightWater 表现提升与 Learned AudioWater 细胞复用

> 说明（写作提示，不作为论文正文）：
> - 已按“上科大学位论文撰写规范（初稿）”将结构改为“第1章…第5章…”形式，并将摘要/Abstract 中的图表引用移除。
> - 图表建议按分章编号（例如“图3.1”“表3.1”），而不是期刊式 Fig.1。
> - 本文件用于组织论述与图表规划，正式排版/页眉页码等由模板完成。

---

## 中文摘要
- 背景：跨任务迁移（从 AudioWater 到 LightWater）可显著影响迁移阶段 LightWater 的行为表现。
- 目的：检验迁移阶段表现提升是否与对既有学习阶段（Learned AudioWater）细胞集合/表征结构的复用相关。
- 方法：结合行为表现、群体表征稳定性指标、单细胞复用指标以及跨天学习速度分析，系统评估迁移机制。
- 结果：迁移阶段表现更高；出现与 Learned AudioWater 一致的细胞集合/表征结构复用特征；并在群体层面表现为更明显的细胞间分化（以细胞间标准差刻画）（部分结论待进一步验证）。
- 结论：结果支持“复用驱动的迁移”框架，并提出可检验的机制预测与后续因果实验方向。

关键词：迁移学习，表征复用，细胞复用，LightWater，钙成像

---

## Abstract
- Background: Cross-task transfer from AudioWater to LightWater may improve behavioral performance in the transfer stage.
- Objective: Test whether improved transfer-stage performance is associated with reusing the neuronal ensemble and representational structure established during Learned AudioWater.
- Methods: We combine behavioral metrics, population-level stability measures, single-cell reuse metrics, and multi-day learning-speed analyses.
- Results: Transfer-stage performance is higher; reuse signatures consistent with Learned AudioWater emerge; and population-level representations show clearer cell-to-cell differentiation (quantified by inter-cell standard deviation) (some analyses remain to be validated).
- Conclusion: These findings support a reuse-driven transfer framework and motivate mechanistic predictions and causal tests.

Keywords: Transfer learning, Representational reuse, Neuronal ensemble, LightWater, Calcium imaging

---

## 第1章 绪论
### 1.1 研究背景与意义
- 迁移是否来自共享表征，还是来自非特异性因素（状态/动机/训练量等）？
- 在动物学习中，“复用已有神经子空间/细胞集合”是迁移学习的重要候选机制。

### 1.2 国内外研究现状与问题提出
- 综述：行为迁移与神经表征复用的相关证据与争议点（待补文献）。
- 本论文聚焦问题：迁移阶段 LightWater 表现提升是否对应 Learned AudioWater 表征的复用。

### 1.3 研究假设、预测与论文结构
- 假设：迁移阶段表现提升与 Learned AudioWater 相关细胞集合在迁移阶段的再激活/复用程度正相关。
- 预测：复用越强的个体/天，迁移阶段行为越好；迁移阶段表征稳定性更高。
- 论文结构：第2章材料与方法，第3章结果，第4章讨论，第5章结论与展望。

---

## 第2章 材料与方法
### 2.1 实验动物与伦理合规
- 小鼠品系、性别、年龄范围、样本量（按组/按实验）。
- 饲养条件：光照周期、温湿度、食水限制/奖励水管理、训练前适应。
- 伦理审批信息：动物实验伦理批准编号与执行规范（写到“可复现”与“合规”所需程度即可）。

### 2.2 手术、病毒与标记/操纵策略
- 开窗/植入、定位坐标、麻醉与镇痛、术后恢复。
- 病毒：载体类型、血清型、启动子、滴度、注射体积/速度、表达时间。
- 活动依赖标记：cFos/TRAP 或 scFLARE 的标记窗口设置与给药/光照参数（如适用）。
- 化学遗传/光遗传操纵：hM4D(Gi) 等工具的表达与验证要点。

### 2.3 行为范式、设备与自动化控制
- 行为任务结构：Cue→(1s)→Water；AudioWater/LightWater 条件；Naive/Learned/Transfer/Final 阶段定义。
- 行为设备：头固定、刺激呈现、给水、lick 检测、同步触发信号。
- 自动化控制：说明系统实现思路与关键参数（时序、触发、日志记录），代码细节不必写进正文。

写作取舍（建议在本节末尾用一句话写清，避免评阅人困惑）：
- 本文的“学习曲线/学习速度”主分析以 LightWater（Naive vs Transfer）为核心；AudioWater 的迁移/学习过程因大量个体首会话即达到 100%（天花板效应）难以稳定刻画过程性指标，故仅作为背景或用于界定 LearnedAudio 阶段与复用参照，不作为主文的过程性学习曲线重点。

关于“纯行为鼠 vs 成像鼠”的合并原则（建议写进方法，避免结果章节被追问）：
- 若两类鼠的 LightWater 行为范式、设备、训练时序与纳排标准一致，则可在第3.1节将其合并作为“行为总体证据”（提升样本量与统计效能）。
- 但需显式处理潜在系统差异（例如手术/头固定耐受、成像日程带来的选择偏差）：
  - 推荐做法：在混合效应模型中加入一个指示变量 `ImagingCohort`（0=纯行为，1=成像/手术），并检验 `Session:Group` 在控制该变量后是否稳健；
  - 同时报告敏感性分析：仅使用成像子集重复关键结论（至少复核图3.1a–d 的方向与显著性）。

### 2.4 双光子钙成像采集
- 指示剂/探针（如 GCaMP）与成像区域（MOp2/5、RSPd2/5 等）。
- 显微镜与采集参数：帧率、视野大小、层深、激光波长/功率、同步到行为的方式。
- 质量控制与排除：运动伪影、光漂白、信噪比、细胞数不足等。

### 2.5 药物与给药方案（如适用）
- DREADD 配体（例如 CNO/替代物）或其它药物：剂量、给药途径、给药到测试的时间间隔。
- 对照条件：vehicle、非特异性表达/操纵对照。

### 2.6 数据预处理与数据库组织
- 成像预处理：配准、ROI 提取、去噪/去混叠、ΔF/F 与基线定义（本文固定 1:24）。
- 行为-成像同步与对齐、Trial 定义与剔除。
- 数据库与查询：Phase/Design/Stimulus/BlockUID 等字段定义；分析均从数据库实时计算。

### 2.7 细胞筛选与表征度量
- 活跃细胞（active cell）定义：基于 QueryNTATS(ZScore, Median) 的中位数曲线，满足 `max(0~1s) > mean(-3~0s) + 3*std(-3~0s)`。
- 群体表征分化（细胞间标准差，DeltaF/ZScore 版本）：
  - 取 1s 的 cell×trial 矩阵 $Z$（每行=细胞，每列=trial）。
  - 先计算每个细胞在 trial 上的平均响应：$m_i = \mathrm{mean}_\mathrm{trial}(Z_{i,:})$。
  - 细胞间标准差：$\mathrm{SD}_{\mathrm{cell}} = \mathrm{std}_\mathrm{cell}(m)$。
  - 解释：$\mathrm{SD}_{\mathrm{cell}}$ 越大表示不同细胞在 1s 处的平均响应差异越大（更“分化/稀疏/集中”）。
- 细胞复用率（建议至少两种口径用于稳健性对照）：
  - 细胞 UID 交集复用（同鼠）：Learned AudioWater 的 active 集合与 Transfer LightWater 的 active 集合交集比例。
  - 表征/功能复用（TBD）：选择性方向一致、或子空间/解码跨条件泛化。

### 2.8 学习速度的定义与检验
你有两条完整多天轨迹：
- Naive LightWater → Learned LightWater
- Transfer LightWater → Final LightWater

为使“增长更快”可检验且可复现，建议并行采用两类定义：

1) 时间到达阈值（time-to-criterion）更短
- 定义行为阈值（例如 performance ≥ 0.8 连续 N blocks/days）。
- 统计：达标曲线（Kaplan–Meier）与 log-rank；或 Cox 模型加入协变量（起点表现、试次数等）。

2) 增长斜率（growth rate）更陡（在起点相同的约束下）
- 关键：控制 baseline performance，避免把“起点更高”误判为“增长更快”。

建议把“学习速度”拆成两层（主分析 + 敏感性分析），以避免因阈值筛选而丢掉大量迁移鼠：

- 主分析（推荐）：不做硬筛选，用模型显式控制起点
  - 线性/广义线性混合效应模型：`Perf ~ Session + Group + Session:Group + BaselinePerf + (1+Session|Mouse)`。
  - 其中 BaselinePerf 建议定义为“该鼠第 1 个 LightWater 会话的 performance”（Naive cohort 与 Transfer cohort 各自的第 1 个 LightWater 会话），Session 从 1 开始。
  - Session:Group 即“增长速度差异”的主检验；BaselinePerf 控制“起点差异”。

- 敏感性分析（阈值对齐/起点匹配的直观版本）：用阈值对齐而不是阈值筛选
  - 以 performance=60% 作为对齐点是合理的（因为你观察到迁移首会话均值约 60%），但不建议直接“排除首会话>60%”——这会系统性丢掉“迁移最强”的个体，导致偏差。
  - 更稳健的做法：定义每鼠的对齐时间 $t=0$ 为“首次达到≥60% 的会话”；对首会话已≥60% 的鼠，直接令 $t=0$（不排除），并在模型中加一个指示变量标注“首会话已过阈”。
  - 对齐后比较 $t\in[0,K]$ 的斜率（或到达更高阈值如 80%/90% 的 time-to-criterion），并报告不同阈值（50/60/70%）与不同 K 的稳健性。

### 2.9 统计分析
- 同数据库同鼠：优先配对（signrank / Friedman）。
- 跨数据库：非配对（ranksum / Kruskal-Wallis）。
- 多重比较：报告 FDR 或 Bonferroni（作为附录表格）。

重要说明（需要在全文保持一致）：
- Naive LightWater 与 Transfer LightWater 不是同一批鼠，因此两者之间的比较必须按“非配对/分层模型”处理。
- 只有同一批鼠、同一条轨迹内的早晚阶段比较（例如 Transfer→Final；Naive→Learned；以及 LearnedAudio→TransferLight）才具备天然配对结构。

### 2.10 操纵实验（因果证据）的组织与分析（新增）
为使“机制”表述从相关走向因果，本研究纳入多类干预：
- 细胞集合特异性抑制：cFos/scFLARE 标记学习阶段活跃细胞，并在迁移阶段用 hM4D(Gi) 抑制。
- 区域非特异性抑制：MOp2/5、RSPd2/5 的泛抑制（化学/光遗传）作为对照。
- 丘脑后部（PO/LP/VPM 等）非特异性抑制：检验“学习速率/增长速度”的调控来源。
- 记忆保持/检索操纵：学会后 7 天 homecage 间隔；以及声水与光水会话内穿插训练。

与钙信号的整合分析要点：
- 若无钙信号：仅报告行为层面的因果影响（对“必要性/特异性”的结论强度需更谨慎）。
- 若有钙信号：进一步检验干预是否改变 0–1s（cue 处理）与 1s 后（策略执行/误差信号/可塑性相关）两个时间窗的表征稳定性、active pool 与复用指标。

### 2.11 代码与数据可用性（盲审友好写法建议）
你把行为控制代码/分析代码放在 GitHub 是加分项，但在“盲审需要去身份”的流程里，直接给出个人 GitHub 链接确实可能被认为不够盲。

建议按你们学院/研究生院对盲审的严格程度，采用以下之一（从保守到开放）：

- 方案A（最保守，盲审阶段完全不放外链）：
  - 正文写法模板：
    - “本研究的行为控制与数据分析代码已归档；为满足盲审要求，评阅阶段不公开外部链接。论文通过后将公开代码与复现说明。”
  - 实操：准备一个可复现的代码归档包（zip，含版本号与运行说明），由学院秘书/系统在盲审材料中单独转交评阅人（评阅人如需）。

- 方案B（半开放，匿名访问但不暴露作者身份）：
  - 使用支持匿名审稿链接的平台（例如 OSF 的匿名 view-only link），或提供不含个人信息的临时镜像仓库；
  - 正文写法模板：
    - “为便于复现，代码在匿名仓库提供（盲审阶段不含作者信息）。论文通过后将替换为正式公开链接。”

- 方案C（若学校的“盲审”主要看论文文本、不要求外链匿名）：
  - 正文只写“代码可获得”，链接放在最终提交版本或答辩后版本；盲审版把链接删掉即可。

不建议：
- 在盲审版本中直接引用带个人/课题组标识的 GitHub 用户名、组织名、或含姓名的 DOI/主页链接。

---

## 第3章 结果

### 3.1 行为表现：迁移阶段 LightWater 的 performance 高于 Naive LightWater
目标句：迁移阶段 LightWater 行为表现显著优于 Naive LightWater。

写作取舍（建议写在本节开头一句话，提前回应“为什么不画声水学习曲线”）：
- 本章行为学习曲线聚焦 LightWater 的 Naive vs Transfer 两个 cohort；AudioWater 由于迁移过快（大量首会话天花板）不利于“过程”比较，且其迁移机制可能与 LightWater 不完全一致，因此仅用于定义 LearnedAudio 参照与复用链条，不作为主文的学习速度主证据。

图3.1：LightWater 的行为优势与学习速度
- 图3.1a：Naive vs Transfer 的首会话 performance 分布（每点=鼠；非配对检验或分层模型）。
- 图3.1b：学习曲线（以会话序号或训练日为横轴，均值±SEM；两 cohort 非配对）。
- 图3.1c：time-to-criterion（例如 80%/90%）的达标天数/达标会话数：Kaplan–Meier 或每鼠散点 + 组间比较（非配对；可控制 BaselinePerf）。
- 图3.1d：增长斜率差异：混合效应模型中 Session:Group 的估计与置信区间（主结论）。
  图3.1 的数据组织说明（建议写入图注或本节文字）：
  - 主分析可使用“全部 LightWater 行为数据”（含纯行为鼠与成像鼠），以最大化对 Naive vs Transfer 行为差异与学习速度的检验效能。
  - 为避免“成像/手术选择偏差”影响结论，需提供敏感性分析：仅成像子集复核图3.1a–d；或在模型中加入 `ImagingCohort` 及其与 Session 的交互项作为控制。
  - 写作说明（不单独作图）：你当前范式下各会话 trial 数相同，“会话内试次数匹配”通常不必强调；更需要说明的潜在混杂是训练以 100% 表现结束导致不同鼠总会话数不同（暴露量不同）。可选的控制写法：
    - 截尾窗口：只比较每鼠前 N 个会话（N 取两组共同覆盖窗口）。
    - 生存/达标模型：将“达到 100%/终止”视为事件（time-to-100% 的 KM/Cox），避免把后续缺失当作缺失机制不明。

  （可选，放附录更合适）图A.x：AudioWater 的行为概览（用于交代“LearnedAudio”参照，而非主文学习曲线）
  - 目的：证明 LearnedAudio 阶段确已达到稳定高表现，并说明 AudioWater 迁移/学习存在天花板效应。
  - 展示建议：NaiveAudio vs LearnedAudio 的分布；以及“首会话=100%”比例（或达标会话数分布），避免硬画过程性曲线导致误导。

### 3.2 单细胞层面：迁移阶段复用 Learned AudioWater 的任务相关细胞集合
目标句：Learned AudioWater 的 active/任务相关细胞在 Transfer LightWater 中更可能再次活跃，且复用程度与行为提升相关。

图3.2：细胞复用与行为的关系（聚焦“LearnedAudio→Transfer”这一条可配对链）
- 图3.2a：代表性单细胞原始信号曲线示意“复用”（同一 CellUID；精心挑选代表细胞）：
  - 展示 4 个条件的 cue 对齐单试次曲线（若干 trial 叠加 + 可选叠加中位数/均值）：
    - Naive AudioWater：不活跃（作为“未复用/未招募”的对照背景）
    - Learned AudioWater：活跃
    - Transfer LightWater Hit：活跃
    - Transfer LightWater Miss：不活跃
  - 信号口径：不要使用/标注 ΔF/F_0；只用 DeltaF 或 ZScore。
  - 基线：以 cue 前 3 秒（-3~0s）作为 baseline（DeltaF=减去 baseline 均值；ZScore=以 baseline 均值/标准差归一化）。
- 图3.2b：Transfer LightWater 会话内 Hit vs Miss 的复用表型（同鼠配对）：
  - 主口径（推荐）：正向复用率（LearnedAudio-active → TransferLight-active）在 Hit 中高于 Miss（signrank）。
  - 可选稳健性：对同一批细胞比较 Hit vs Miss 的响应幅度与起步时间（例如 0~1s 峰值、峰值延迟），并控制 Hit/Miss trial 数。
- 图3.2c：复用率与迁移行为优势的相关性（Spearman；并报告稳健性：控制 NCells/NTrials）。
- 图3.2d：Transfer 并不比Naive绝对活跃水平更高（全细胞平均钙曲线几乎重合），而是表现为全细胞方差更大，即细胞之间的分化、信息集中流向少数细胞。这种效应是复用细胞带来的，扣除后严重削弱。Naive命中和错失回合的全细胞方差差异显著，而 Transfer Performance 也和全细胞方差高度相关（Naive不适合做Performance相关性，因为有太多Performance为0的鼠）

### 3.3 群体层面：迁移阶段细胞间分化（细胞间标准差）
目标句：迁移阶段 LightWater 的 trial-to-trial 变异更低，群体表征更稳定。

承接逻辑（建议写进本节开头 1–2 句话）：
- 上一节先在单细胞层面建立“复用”的直接证据；本节再用不依赖细胞一一对应的群体指标（细胞间标准差）给出复用的一种统计表型与跨 cohort 的可比证据。

图3.3：五组细胞间标准差对比（NaiveLight、NaiveAudio、LearnedAudio、TransferLight、FinalLight）
- 图3.3a：代表性单细胞响应分布示意“细胞间标准差”（cell-to-cell heterogeneity）：同一会话在 1s 处，对每个细胞取 trial 均值后得到 $m$，展示 $m$ 的分布宽/窄。
  - 图示要点：细胞间标准差大→细胞间平均响应差异更大（更分化/更稀疏）；细胞间标准差小→响应更均匀。
- 图3.3b：五组细胞间标准差分布（每点=鼠），标注均值±SEM。
- 图3.3c：关键两两比较结果（同库配对、跨库非配对；并给出效应量）。
- 图3.3d：细胞间标准差度量示意（cell×trial → per-cell trial-mean → cell-to-cell std）。

关于“细胞间标准差让 Naive/Transfer 可比”的解释（建议写入结果或讨论的开头）：
- 复用率需要细胞跨天/跨任务追踪，因此 Naive LightWater（缺少对应的 LearnedAudio）无法定义“跨任务复用率”。
- 但细胞间标准差是纯群体统计量，不依赖细胞一一对应，因此可用于比较不同 cohort 的 LightWater 群体响应分化程度。
- 需要强调：细胞间标准差差异本身不等同于“复用导致差异”，它只是提供了一个让 Naive 与 Transfer 在同一坐标系下可比的群体表型。

关于“为什么细胞间标准差在数学上可能与复用有关？”（建议写入本节作为可检验预测，而非直接下因果结论）：
- 细胞间标准差可理解为：在同一会话/条件下，细胞在关键时刻（例如 1s）的“平均响应差异”有多大。
- 若迁移阶段复用了“Learned 阶段中一小群高响应的细胞”，则这些细胞的 $m_i$ 会显著高于其余细胞，从而拉大 $m$ 的分布宽度，使细胞间标准差增大。
- 因此细胞间标准差升高与复用更一致，是一个**方向性预测**：复用越强 → 细胞间标准差越大（在控制细胞数/试次数等因素后）。

建议的验证（把“细胞间标准差—复用”从直觉变成可检验）：
- 在 transfer cohort 内，把细胞按“复用 vs 非复用”分组，比较两组在 TransferLight 中的：
  - 响应强度（例如 1s 处的 per-cell trial-mean）；
  - 对整体细胞间标准差的贡献（例如只用复用细胞子集计算 $\mathrm{SD}_{\mathrm{cell}}$，或替换/置换非复用细胞后再算）。
- 做一个“贡献分解/替换实验”（统计层面即可，不需要真的操纵）：用复用细胞子集单独计算细胞间标准差、或把非复用细胞随机采样到同样 NCells，再看细胞间标准差是否仍显著更大。

### 3.4 钙信号机制：哪些神经现象与“增长更快”相关？
写法建议：陈述为“伴随出现/可预测”，避免直接因果化。

图3.4：随天机制轨迹与相关性（强调“同轨迹内配对变化”，减少 cohort 间不可比争议）
- 图3.4a：细胞间标准差随天变化：Naive→Learned（同 cohort 内）与 Transfer→Final（同 cohort 内）分别计算，再比较两条轨迹的变化斜率（轨迹间为非配对，但每条轨迹内部是配对随天）。
- 图3.4b：active pool 的 day-to-day 稳定性随天变化（同上）。
- 图3.4c：LearnedAudio→Transfer 的复用/对齐指标随天变化（若有跨天细胞追踪）。
- 图3.4d：跨鼠相关：行为增长斜率 vs（细胞间标准差变化斜率/active pool 稳定化速度/复用增长速度）。

关于“NaiveLight vs LearnedLight 是否有意义？”的定位（建议在本节或讨论里写清）：
- 有意义：它提供“同一任务从生疏到熟练”的内部对照，用于说明稳定性/active pool 如何随学习变化。
- 但它的“复用”是同任务跨天的 pool 稳定/再招募，并不等同于 LearnedAudio→TransferLight 的跨任务复用；两者可并列呈现，但要避免把数值当成同一量纲直接比较。

关于“FinalLight vs LearnedLight 是否有意义？”
- 这两个阶段通常来自不同 cohort（Transfer 轨迹的 Final vs Naive 轨迹的 Learned），因此只能做非配对比较。
- 更建议把它们用作“各自轨迹的末期状态”进行描述性对照，并把主推断放在同轨迹内（Naive→Learned、Transfer→Final）的变化上。

### 3.5 表征几何与控制分析（可选：可并入附录，减少主图数量）
图3.5（建议作为“可选增强/附录主图”，看你最终数据成熟度）：
- 子空间/解码泛化：LearnedAudio 训练的子空间/解码在 TransferLight 上的解释率或 AUC。
- 控制分析：NCells、NTrials、样本量匹配、active-only 口径稳健性。

### 3.6 因果证据：抑制“复用相关细胞/通路”会削弱迁移表现或减慢增长（新增）
本节目标：把“复用机制”从相关性提升到**必要性/特异性**层级（在数据允许范围内）。

表3.1：操纵实验总览（建议做成一页总表，审稿人非常爱看）
- 标记策略：cFos / scFLARE / 无标记
- 作用靶点：MOp2/5、RSPd2/5、丘脑后部
- 操纵类型：hM4D(Gi) 化学抑制 / 光遗传抑制
- 是否有钙信号：有/无
- 主要效应：首次迁移表现、后续增长速度、是否可逆

图3.6：操纵实验的因果证据链（按“特异性→对照→网络层级→记忆操纵”组织；主图之一）
- 图3.6a（细胞集合特异性，强证据）：
  - cFos 标记 Learned AudioWater 阶段 MOp2/5 活跃细胞；迁移阶段抑制后迁移行为下降。
  - 写作表述建议：可用于支持“迁移依赖于学习阶段招募的 MOp 细胞集合（必要性证据）”，但需注明无钙信号。
- 图3.6b（非特异性对照，提升特异性）：
  - MOp2/5 直接表达 hM4D(Gi) 的非特异性抑制在迁移阶段无效。
  - 表述意义：支持“不是简单的 MOp 整体下调导致行为变差”，而更符合“特定细胞集合/通路”的机制。
- 图3.6c（scFLARE 标记 + 抑制 + 有钙信号，虽然 n 小但信息量极高）：
  - 标记学会阶段 cue 后 0–3s 活跃细胞；迁移阶段抑制导致两只鼠出现“增长受阻/可逆恢复”的现象。
  - 钙信号现象：抑制条件下约 1s 左右信号低于正常迁移。
  - 表述建议：将其定位为“机制一致性 + 初步因果证据（pilot）”，强调需要扩大样本量。
- 图3.6d（网络节点：丘脑后部抑制，强烈影响增长速度）：
  - 现象：首次迁移略降，但后续增长速度大幅下降；钙信号 1s 后显著低于正常。
  - 机制猜测（可写入讨论并在本节作为结果描述）：丘脑后部可能更像“学习速率/可塑性门控”而非“初始迁移表现”的决定因素。
  - 与复用关系的张力：复用率差异大但初次表现相近，提示“复用不是唯一瓶颈”，存在并行通路。
- 图3.6e（记忆保持/检索操纵：7 天 homecage 间隔）：
  - 现象：迁移大幅下降，复用率也显著降低；并有钙信号。
  - 机制表述建议：支持“复用表征需要被保持/可被检索”，间隔可能导致表征可用性下降或被新状态重置。
- 图3.6f（任务结构操纵：会话内穿插旧任务与新任务，出现双峰结局）：
  - 现象：部分鼠快速学会，部分鼠长期学不会；有钙信号待分析。
  - 机制猜测：可能存在“策略竞争/状态切换失败/吸引子选择”导致的双稳态学习结局（详见第4章讨论的创新假说）。

（可选，放附录更合适）图A.x：若要突出 RSPd：RSPd2/5 在正常迁移中对光水响应更强、时间更早
- 写作意义：提示 RSPd 更偏“感觉 cue 编码/教学信号”，而 MOp 更偏“策略执行/复用表征”；并解释“行为与 MOp 复用相关、而与 RSPd 复用未必相关”的可能原因。

---

## 第4章 讨论
### 4.1 主要发现总结
- 迁移阶段 LightWater 表现更高。
- 观察到与 Learned AudioWater 一致的复用特征。
- 群体层面出现更明显的细胞间分化（以细胞间标准差刻画）。

### 4.2 可能机制与创新性猜想
- 猜想：Learned AudioWater 建立了动作—结果的稳定动力学骨架；迁移时只需重映射感觉输入。
- 猜想：复用细胞更偏策略/动作序列编码，而非纯感觉编码（可用运动学回归检验）。

建议在讨论中明确“多组件机制”（与第3.6节对齐）：
- 组件A（MOp 细胞集合）：更决定“初始迁移表现/策略可用性”（细胞集合特异性抑制有效，非特异性抑制无效）。
- 组件B（丘脑后部）：更决定“增长速度/可塑性门控”（主要影响后续增长，且 1s 后钙信号显著受损）。
- 组件C（RSPd）：更偏“感觉 cue 的早期表征/教学信号”（响应更早更强，但与 MOp 复用关系可能弱）。

### 4.3 局限性
- 复用的因果性尚未证明。
- 细胞追踪与配准误差可能影响 overlap。

可补充一条更贴合现状的局限性表述：
- 部分因果实验缺少钙信号或样本量较小（例如 scFLARE n=2），因此“必要性/特异性”的结论强度在不同实验间不一致，需要分层陈述。

---

## 第5章 结论与展望
### 5.1 结论
- 总结本文主要结论与创新点（待依据最终分析定稿）。

### 5.2 展望：从相关到因果的后续研究
- 因果干预：在阶段切换当天选择性抑制 Learned 细胞集合，观察是否拖慢增长速度。
- 任务顺序操控：Audio→Light vs Light→Audio，检验“先学任务是否提供可复用骨架”。
- 难度阶梯：检验迁移优势是否在高难度阶段仍存在。

---

## 参考文献
- 按 GB/T 7714—2015 格式整理（待补）。

## 附录
### 附录A 图表与分析规划（写作提示，不入正文）
- 图表编号建议：按章编号（图3.1、表3.1），并在图题中提供中英文（如学校模板要求）。
- 附录表A.1：两两比较 p 值汇总（含多重比较校正）。
- 附录图A.1：细胞间标准差与 active 判据的计算示意。
- 附录图A.2：学习速度稳健性分析（起点匹配、不同阈值、不同 K 天窗口、混合效应模型细节）。
- （可选）附录图A.3：AudioWater 行为概览与天花板效应说明（用于界定 LearnedAudio 参照，不作为主文学习速度主证据）。

### 附录B 待验证清单（TBD Checklist，不入正文）
1) 行为指标确定并导出：每鼠 Naive LightWater vs Transfer LightWater 的 performance。
2) 绘制图3.2：复用示例 + Hit/Miss（同会话配对）+ 复用率与行为相关；并补充 Transfer vs Naive 的“活跃更强/更早”及其是否由复用细胞承载。
3) 绘制图3.3：五组细胞间标准差的主图与显著性标注。
4) 绘制图3.4：机制随天变化（细胞间标准差/active pool/复用指标）与增长速度相关。
5) 可选：图3.5 表征几何/解码泛化与控制分析；附录补充 active-only 与样本量匹配稳健性。
6) 绘制图3.6 与表3.1：操纵实验因果证据链与总览表。

## 致谢
- （待补）

## 作者简历及攻读学位期间发表的学术论文与研究成果
- （待补）
