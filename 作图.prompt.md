# MATLAB 作图规范

本项目使用以下作图规范，确保图表风格一致。

## 图窗尺寸

使用 `MATLAB.Graphics.FigureAspectRatio(width_mm, height_mm, scale)` 设置图窗尺寸，单位为毫米。

常用尺寸参考：
- 学习曲线类：90×80mm
- 条形图（简单）：30×20mm
- 热图：120×80mm
- 配对散点图：30×40mm
- 相关性散点图：30×40mm
- 代表性细胞图：45×40mm

## 颜色规范

通过 `GlobalOptimization.ColorAllocate` 获取统一颜色：
- Naive/Initial：红色 `[1, 0, 0]`
- Transfer：蓝色 `[0, 0, 1]`

## 条形图样式

```matlab
bar(ax, x, y, 'FaceColor', color, 'FaceAlpha', 1/3, 'EdgeColor', color, 'LineWidth', 0.5);
errorbar(ax, x, y, err, 'k', 'LineStyle', 'none', 'LineWidth', 0.5, 'CapSize', 3);
```

- `FaceAlpha = 1/3`：半透明填充
- `LineWidth = 0.5`：边框和误差线细度

## 散点图样式

空心散点：
```matlab
scatter(ax, x, y, markerSize, color, 'LineWidth', 0.2);  % 空心圆，边框0.2
```

实心散点：
```matlab
scatter(ax, x, y, markerSize, color, 'filled');
```

## 坐标轴设置

```matlab
box(ax, 'off');
grid(ax, 'off');  % 通常关闭网格
ax.FontSize = 6;
```

## 拟合线

```matlab
plot(ax, xFit, yFit, '-', 'LineWidth', 1, 'Color', color);  % 实线
```

## SVG 导出

使用 `TransferLearning.PrintFigure(f, svgPath)` 导出 SVG 格式。

输出目录通常为：`\\Data-Server-2\个人数据\张天夫\202601`

## 统计标注

使用 `MATLAB.Graphics.PLine` 添加 p 值标注线和星号。

## 数据层合并

当不区分 2/3 层和 5 层时，将两层数据合并后取每鼠均值作为统计单位。
