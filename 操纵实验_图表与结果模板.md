# 操纵实验：图表与结果写作模板（表3.1、图3.8、图3.9）

> 目标：把操纵实验写成“因果证据链”，并且让你只需要把数据填进表格/脚本，就能直接生成图与结果段落。
>
> 约定：
> - 迁移任务：AudioWater → LightWater。
> - “首次迁移表现”= Transfer LightWater 第1会话（或 Day0）performance。
> - “增长速度”= Day0→DayK 的斜率（或 time-to-criterion）。
> - 若存在重复测量（同鼠多天）：优先混合效应模型；若样本较小：采用非参数配对/非配对 + 展示个体轨迹。

---

## 表3.1 操纵实验总览（强烈建议主文放一页总表）

### 表结构（建议列）
- `ExpID`：实验编号（你自定义）
- `Task`：声→光 / 光→声 / 穿插 / 间隔7天等
- `Labeling`：cFos / scFLARE / none
- `Target`：MOp2/5 / RSPd2/5 / TH posterior (PO/LP/VPM...) / multi
- `Manip`：hM4D(Gi) / opto
- `Specificity`：ensemble-specific / region-wide
- `Calcium`：Yes/No；Region=MOp/RSPd/TH
- `N_mice`：样本量
- `Effect_FirstTransfer`：↑/↓/无效（带方向）
- `Effect_GrowthRate`：↑/↓/无效（带方向）
- `Reversible`：Yes/No/Unknown（停药后是否恢复增长）
- `KeyNeuralObs`：一句话（如“~1s信号下降”“1s后大幅降低”）
- `Notes`：局限性（例如光纤出光范围、n=2）

### 主文表述模板（用于结果开头）
- “为检验迁移学习中复用相关神经活动的因果作用，我们在迁移阶段对不同细胞集合/脑区进行抑制操纵（表3.1），并评估其对首次迁移表现与后续增长速度的影响。”

---

## 图3.8 因果证据链（推荐面板顺序：特异性→对照→pilot钙→网络节点→记忆操纵→任务结构）

> 图形风格建议：每个面板统一使用“每鼠一条线”的展示（pre vs post、drug on vs off、或 day0–dayK 轨迹），能显著增强因果说服力。

### 图3.8a 细胞集合特异性抑制（cFos 标记 Learned AudioWater → 迁移期抑制）
**问题**：Learned 阶段活跃的 MOp ensemble 是否对迁移期行为是必要的？

**数据需求（最少）**
- `Mouse`
- `Group`：cFos-ensemble hM4D(Gi) vs 对照（例如 mCherry 或 saline）
- `Session/Day`
- `Performance`
- （可选）`CNO`：on/off

**推荐统计**
- 若有同鼠对照（on/off）：配对 `signrank` + 报告效应量（median diff）。
- 若组间：`ranksum`；并用混合效应模型控制 Day（若多天）。

**作图**
- 主图：Day0（首次迁移）performance 的每鼠散点 + 均值/SEM；或 on/off 连线。
- 可选：多天曲线（若有），但别淹没重点。

**图注模板**
- “图3.8a 选择性抑制 Learned AudioWater 阶段在 MOp2/5 中被 cFos 标记的细胞集合，会降低迁移阶段 LightWater 的行为表现。”

**结果句式模板**
- “与对照相比，cFos 标记 ensemble 抑制组在首次迁移会话中的 performance 显著降低（统计方法；p=…），提示迁移行为依赖于学习阶段招募的特定细胞集合。”

**审稿人会问的控制点（建议在图3.8b回答）**
- “这是不是只是 MOp 整体抑制导致？”

---

### 图3.8b 非特异性对照（MOp2/5 region-wide 抑制无效）
**问题**：迁移下降是否来自 MOp 全局活动降低（非特异性效应）？

**数据需求**
- `Mouse`, `Group`：MOp-wide hM4D(Gi) vs 对照
- `Performance`（至少 Day0）

**作图**
- 与图3.8a保持同一图型（散点/箱线/连线）。

**图注模板**
- “图3.8b 与细胞集合特异性抑制不同，MOp2/5 的非特异性化学抑制对迁移行为无显著影响。”

**结果句式模板**
- “MOp2/5 的非特异性抑制未显著改变首次迁移表现（p=…），支持迁移受损并非由 MOp 整体下调引起，而更符合特定 ensemble/通路的必要性。”

---

### 图3.8c scFLARE 标记 ensemble 抑制（有钙信号，n 小但信息密度高）
**问题**：标记“学会阶段 cue后0–3s 活跃细胞”的 ensemble 是否对迁移增长速度有因果作用？

**数据需求**
- `Mouse`（n=2也可以，关键是轨迹）
- `Condition`：Normal transfer vs Inhibition (CNO on) vs Recovery (CNO off)
- `Day`（至少 Day0–Day3）
- `Performance`
- `Calcium`：MOp2/5 trial-averaged traces（建议保留单trial用于变异分析）
- （可选）`ReuseMetric`：每鼠复用率（解释“首次迁移高”）

**推荐统计（n=2时的写法）**
- 不建议强调显著性；主打“可逆性 + 一致方向”。
- 采用“单鼠轨迹 + 描述性统计 + 与既有结论一致”。

**作图（建议两行）**
- 上排：行为：每鼠 performance 随天曲线（Normal / CNO on / off）。
- 下排：钙信号：
  - 0–1s 与 1–3s 的平均响应幅度条形/散点（每鼠/每天）
  - 或给一张“代表性 session 的平均轨迹（mean±SEM）”。

**图注模板**
- “图3.8c 抑制 scFLARE 标记的学习相关细胞集合可选择性阻断迁移后的表现增长，且停药后增长恢复；该行为效应伴随 MOp2/5 在约1s 附近的钙信号下降。”

**结果句式模板**
- “尽管样本量有限（n=2），在抑制期两只动物均出现增长受阻/异常（其中一只表现为…，另一只表现为…），且停药后增长恢复，提示标记 ensemble 对迁移学习进程可能具有因果作用。”

**可追加的“钙信号深挖”分析（强烈推荐）**
- 抑制期 vs 正常期：
  - 0–1s/1–3s 分窗的：平均幅度、peak latency、trial-to-trial 变异（你的散度口径可直接用）
  - active pool 大小与 day-to-day 稳定性
  - 复用率是否下降，还是“复用仍在但强度变弱/时序变慢”

---

### 图3.8d 丘脑后部（PO/LP/VPM等）抑制：主要影响增长速度（有钙信号）
**问题**：是否存在“学习速率门控”节点，专门影响后续增长而非首次迁移？

**数据需求**
- `Mouse`, `Group`：TH inhibition vs control
- `Day`：至少 Day0–DayK
- `Performance`
- `Calcium`：你已观察到“1s后大幅降低”，建议保留原始 trial

**关键分析（把你的观察变成定量结论）**
- 行为：
  - 首次迁移（Day0）差异：小/不显著
  - 增长斜率或 time-to-criterion：显著变慢
- 钙信号：
  - 0–1s 与 1–3s 分窗幅度（重点比较 1–3s）
  - 散度随天下降是否被“卡住”（stabilization rate 下降）

**作图（建议三联）**
- 左：Day0 performance（散点）
- 中：学习曲线（Day0–DayK）
- 右：钙信号分窗变化（尤其 1–3s）

**图注模板**
- “图3.8d 抑制丘脑后部显著减慢迁移后的增长速度，并伴随 1s 后钙信号与表征稳定化过程的显著受损。”

**结果句式模板**
- “丘脑后部抑制对首次迁移表现影响有限，但显著降低后续增长斜率/延长达标时间（统计；p=…）。对应地，抑制条件下 1s 后钙信号幅度显著降低且散度下降受阻，提示丘脑后部更可能参与学习速率/可塑性门控。”

**可解释“复用率不相关”的分析方向**
- 把复用拆成两部分：
  - “结构复用”（UID overlap） vs “强度/可用性”（复用细胞的响应幅度、centroid-to-origin）
- 你可能会发现：UID overlap 不变，但复用细胞的“有效信号强度”下降，从而行为增长变慢。

---

### 图3.8e 7天 homecage 间隔：迁移下降 + 复用下降（有钙信号）
**问题**：复用机制是否依赖表征的保持/可检索性？

**数据需求**
- `Mouse`, `Group`：No break vs 7-day break
- `Performance`：至少 Day0
- `Calcium`
- `ReuseMetric`：复用率（你已观察下降）

**关键分析**
- 行为下降是否主要体现在：
  - 初次迁移表现（起点）下降？
  - 增长速度也下降？（建议顺手测）
- 神经：
  - 复用率下降（结构）
  - 复用细胞幅度/时序是否也改变（功能/可用性）

**图注/结果句式模板**
- “7天间隔显著削弱迁移表现，并伴随复用率下降与表征稳定性降低，支持迁移依赖可被检索的学习相关表征。”

---

### 图3.8f 会话内穿插旧任务与新任务：双峰结局（有钙信号，待分析）
**问题**：任务结构如何影响迁移？双峰是否来自策略竞争/状态切换失败？

**数据需求**
- `Mouse`
- `OutcomeGroup`：Fast learners vs Non-learners（按达标天数或最终是否学会二分）
- `WithinSession`：trial-level 标记（Old task vs New task）
- `Calcium`：至少 MOp；若有 RSPd 更佳

**强烈推荐的分析（能做成亮点）**
- “表征状态分类”：在穿插会话内，把 trial 按神经活动投影到低维空间，判断更像旧任务还是新任务。
  - 指标：两簇分离度、切换概率、是否长时间停留在旧簇。
- “双稳态模型”：Fast learners 更快进入新任务吸引子；Non-learners 被旧任务吸引子锁定。

**作图建议**
- 左：行为学习曲线（fast vs non）
- 中：低维轨迹/聚类示意（同一会话内两簇 + 切换）
- 右：切换指标与学习速度的相关

---

## 图3.9（可选）RSPd 的时序优势：更早更强的光水响应
**使用条件**：你希望把机制写成“RSPd 负责 cue/教学，MOp 负责策略/复用输出”的多节点模型。

**数据需求**
- 同鼠同时记录 MOp 与 RSPd（或至少同范式可比）
- 0–3s cue 对齐的平均轨迹

**核心量化指标（越简单越好）**
- peak latency（RSPd vs MOp）
- 0–1s 平均幅度（RSPd vs MOp）
- 这些指标与行为（起点/斜率）的相关性

---

## 附：把“钙信号可能还有其它特征”系统扫一遍（建议优先级）

> 你已经看到“~1s下降”“1s后大幅下降”“复用率变化”。下面是我建议的“高产出指标清单”，优先做前5项。

1) **时间窗分解（0–1s vs 1–3s）**：幅度、peak latency、AUC
2) **表征稳定性随天变化**：散度（你们已定义口径）与其下降斜率
3) **复用的‘结构-强度-时序’三分解**：
   - 结构：UID overlap/Jaccard
   - 强度：复用细胞的 centroid-to-origin / 平均响应幅度
   - 时序：复用细胞 peak latency 是否延迟
4) **active pool 稳定性**：day-to-day overlap + active 数量达到平台的速度
5) **状态切换/双稳态（穿插最适用）**：同一会话内旧/新表征簇的切换概率
6) **行为变量回归（若有运动学/lick）**：用 GLM 剥离运动成分，看“学习相关残差”是否被抑制

---

## 你下一步最省力的落地方式（建议）
- 先把“有钙信号”的三类关键操纵（scFLARE、丘脑后部、7天间隔、穿插）统一导出一个长表（long-format）：
  - 每行：`Mouse, Day, Trial, Condition, Region, Time, Signal, Performance`（或聚合后每行是 day-level 指标）
- 我可以随后帮你把这些指标计算脚本（MATLAB）按同一接口写出来，并直接产出图3.8c/d/e/f需要的面板。
