# 1. 当前模型结构概览

模型包含四类核心活动群体：

- `CueIn`：感觉线索输入，不计入 L2/3 活动；
- `L23`：接收 `CueIn` 经由可塑性投射 `W_CueInputToL23` 形成的 L2/3 表征；
- `Reward`：独立奖励输入细胞；
- `L5RewardRecv`：接收奖励输入的 L5 群体；
- `L5Read`：行为读出 L5 群体。

当前 L5 组成是：

```matlab
Params.NL5Read = 64;
Params.NL5RewardRecv = 2 * Params.NL5Read;
Params.NL5 = Params.NL5RewardRecv + Params.NL5Read;
```

因此：

- `L5RewardRecv = 128`；
- `L5Read = 64`；
- 总 L5 为 `192`；
- 奖励接收群体占全部 L5 的 $2/3$。

模型中的主要可塑性连接包括：

```matlab
Mouse.W_CueInputToL23
Mouse.W_RewardToL5RewardRecv
Mouse.W_L23L5ToL23L5
```

其中 `W_L23L5ToL23L5` 是跨 L2/3 和 L5 的 recurrent E-E 矩阵，包含：

- L2/3 → L2/3；
- L2/3 → L5RewardRecv；
- L2/3 → L5Read；
- L5RewardRecv → L2/3；
- L5RewardRecv → L5Read；
- L5Read → L2/3；
- L5Read → L5RewardRecv；
- L5Read → L5Read。

对角自连接被固定为 0。

---

# 2. 响应异质性如何被定义

每个 session 内，模型记录 cue decision phase 的群体响应。

当前代码中 L2/3 响应异质性来自：

```matlab
Signals.ProcessMeanL23 = mean(rL23_cue_all, 2, 'omitnan');
```

L5 响应异质性来自：

```matlab
processMeanL5RewardRecv = mean(rL5RewardRecv_cue_all, 2, 'omitnan');
processMeanL5Read = mean(rL5Read_cue_all, 2, 'omitnan');
Signals.ProcessMeanL5 = [processMeanL5RewardRecv; processMeanL5Read];
```

随后异质性指标用 `iRestrictedStd` 计算，即对细胞平均响应向量取标准差：

```matlab
resultMeanH23 = iRestrictedStd(finalMeanL23);
resultMeanH5  = iRestrictedStd(finalMeanL5);
```

所以这里的“响应异质性”不是 trial-to-trial variability，而是：

> 在学习过程中，同一群体内不同细胞的平均 cue-evoked activity 分布有多分散。

若只有少数细胞强响应、其他细胞弱响应，异质性高；若所有细胞响应接近，异质性低。

---

# 3. L5 响应异质性的产生机制

当前模型中，L5 异质性主要由四个来源共同产生。

## 3.1 初始随机权重分布

每只 mouse 的 afferent 和 recurrent 权重均随机初始化：

```matlab
Mouse.W_CueInputToL23 = randn(Params.NL23, Params.NCueInput) / sqrt(Params.NCueInput);
Mouse.W_RewardToL5RewardRecv = randn(Params.NL5RewardRecv, Params.NReward) / sqrt(Params.NReward);
Mouse.W_L23L5ToL23L5 = iZeroSelfProjection(sd * randn(Params.NL23L5, Params.NL23L5));
```

因此，不同 L2/3 和 L5 细胞从一开始就具有不同的 input alignment。

在同一个 cue pattern 下，有些细胞天然更容易被驱动，有些细胞较弱；这种差异经 Hebbian 更新后会被放大。

---

## 3.2 Hebbian 正反馈放大初始差异

每个 trial 的 learning phase 中，模型更新：

```matlab
Mouse.W_CueInputToL23 = iHebbAfferent(Mouse.W_CueInputToL23, rL23_L, cueInput_L, eta, Params.AfferentWCap);
Mouse.W_RewardToL5RewardRecv = iHebbAfferent(Mouse.W_RewardToL5RewardRecv, rL5RewardRecv_L, rReward_L, eta, Params.AfferentWCap);
Mouse.W_L23L5ToL23L5 = iHebbNoSelf(Mouse.W_L23L5ToL23L5, internalActivity_L, eta, Params.WCap);
```

也就是说，活动越强的细胞越容易获得更强连接。

这会形成典型的 positive feedback：

1. 初始权重使某些细胞对 cue/reward/readout 更敏感；
2. 这些细胞在 learning phase 中响应更强；
3. Hebbian 更新进一步增强它们的输入和 recurrent 连接；
4. 后续 cue decision phase 中，这些细胞更容易被激活；
5. 群体响应分布变得更分散，异质性升高。

这个过程由随机初始权重和活动依赖可塑性自然产生，不依赖额外的 mouse-level 学习率调制项。

---

## 3.3 奖励输入选择性驱动 L5RewardRecv

学习阶段中，reward input 通过 `W_RewardToL5RewardRecv` 驱动 L5 reward-receiving cells：

```matlab
preReward_L = rewardInputLevel * Params.RewInputGain * Mouse.RewardPattern + Params.NoiseRew * randn(Params.NReward, 1);
rReward_L = iRunArea(preReward_L, 'reward', Mouse, Params);
preL5RewardRecv_L = (Mouse.W_RewardToL5RewardRecv * rReward_L) / Params.RewardAfferentNorm + Params.NoiseRew * randn(Params.NL5RewardRecv, 1);
```

由于 `W_RewardToL5RewardRecv` 是随机矩阵，不同 L5RewardRecv 细胞对同一个 reward pattern 的投影不同。

因此奖励不是均匀地激活所有 L5RewardRecv 细胞，而是选择性激活其中一部分细胞。被 reward 更强驱动的 L5RewardRecv 细胞会在 Hebbian 更新中获得更强 recurrent coupling。

这部分机制尤其影响 L5 异质性，因为当前 L5 中 `L5RewardRecv` 占 $2/3$，奖励接收群体数量是读出群体的 2 倍。

---

## 3.4 L5Read 教学信号把 reward-related L5 状态接到行为读出

learning phase 中，L5Read 接收 readout target pattern：

```matlab
preL5Read_L = Params.ReadInputGain * Mouse.L5ReadoutPattern + Params.NoiseRead * randn(Params.NL5Read, 1);
```

随后 `internalActivity_L = [rL23_L; rL5RewardRecv_L; rL5Read_L]` 被用于 recurrent Hebbian 更新。

这使得同一 trial 中共同活跃的：

- cue-evoked L2/3 pattern；
- reward-driven L5RewardRecv pattern；
- readout-driven L5Read pattern；

被绑定到同一个 recurrent schema 中。

因此，后续 decision phase 即使不提供 reward input，也可以通过 cue → L2/3 → recurrent L5 的路径重新激活一部分 L5RewardRecv 和 L5Read pattern。

这就是 L5 cue-evoked heterogeneity 的主要来源。

---

# 4. 为什么 Transfer 的 L5 异质性高于 Naive

`Transfer` 与 `Naive` 的关键差别不是新任务阶段的规则不同，而是 `Transfer` 在进入新任务前已经经历了 pretraining。

## 4.1 Transfer 继承了预训练形成的 schema

对 `Transfer` 和 `THOff`，模型先执行：

```matlab
Mouse = iPretrainMouse(Mouse, Params);
```

预训练使用 `PreCueInputPattern`，并且 reward input 完整保留：

```matlab
pretrainCond.RewardInputLevel = 1.00;
```

预训练期间同样更新：

- `W_CueInputToL23`；
- `W_RewardToL5RewardRecv`；
- `W_L23L5ToL23L5`；
- inhibitory gains。

因此，在新任务开始前，Transfer mice 已经拥有一个可复用的 recurrent schema：

```text
cue-like L2/3 activity ↔ reward-related L5RewardRecv activity ↔ L5Read activity
```

Naive mice 则没有这个预先形成的 schema。

---

## 4.2 新旧 cue pattern 有重叠

模型设置：

```matlab
Params.CueModalityCorr = 0.50;
```

`PreCueInputPattern` 和 `CueInputPattern` 共享一部分 sensory dimensions：

```matlab
Mouse.PreCueInputPattern = iStandardize(a * sharedCue + sqrt(1 - a^2) * preCueU);
Mouse.CueInputPattern    = iStandardize(a * sharedCue + sqrt(1 - a^2) * cueU);
```

因此，新任务 cue 并不是完全陌生输入。它能部分激活预训练时已经强化过的 L2/3 子空间。

一旦这个 L2/3 子空间被激活，recurrent matrix 就会把 activity 传播到预训练绑定过的 L5RewardRecv 和 L5Read 子空间。

---

## 4.3 Transfer 的 L5 response 从第一阶段就被 schema 放大

Naive 的新任务初期：

```text
new cue → random W_CueInputToL23 → weak/random L2/3 → weak/random L5
```

Transfer 的新任务初期：

```text
new cue → partially overlapping L2/3 subspace → pretrained recurrent schema → L5RewardRecv/L5Read reactivation
```

因此 Transfer 在 cue decision phase 中已经可以产生更结构化、更选择性的 L5 activation。

这种选择性 activation 会提高细胞间平均响应的标准差，即提高 L5 heterogeneity。

---

## 4.4 L5RewardRecv 数量扩张进一步放大 Transfer-Naive 差异

当前 L5RewardRecv 是 L5Read 的 2 倍：

```text
L5RewardRecv : L5Read = 2 : 1
```

因此，L5 heterogeneity 主要受 reward-receiving L5 population 的影响。

Transfer 的预训练会显著塑造 `W_RewardToL5RewardRecv` 和 recurrent L5RewardRecv connections；Naive 初期没有这部分历史。

所以在 L5 总体指标中，Transfer 更容易表现为：

```text
更强的 reward-related L5 subspace reactivation
→ 更分散的 L5 cell-wise response distribution
→ 更高的 L5 heterogeneity
```

---

# 5. L5 异质性与 Transfer 学习斜率快于 Naive 的关系

Transfer 学习斜率更快，主要不是因为它有更高的单 trial learning rate。当前三组共同使用：

```matlab
eta = Params.HebbRate;
```

也就是说，Naive 与 Transfer 的 per-trial Hebbian rate 相同。

Transfer 更快的原因是：

> Transfer 的初始状态更接近可产生 hit 的 attractor/readout schema，因此同样大小的 Hebbian 更新能更快转化为行为表现提升。

---

## 5.1 Transfer 有更高的初始 readout drive

Transfer 继承预训练后，cue decision phase 更容易激活 L5Read。行为判断是：

```matlab
decCue  = mean(Mouse.L5ReadoutPattern .* rL5Read_cue);
decision = decCue;
isHit(t) = decision >= Params.HitThreshold;
```

因此，只要 L5Read activity 与 `Mouse.L5ReadoutPattern` 的 alignment 增强，hit rate 就会上升。

Transfer 的 recurrent schema 已经包含 L5Read target pattern，因此新 cue 只需要通过部分重叠的 L2/3 activity 接入这个 schema，就能较快跨过 threshold。

---

## 5.2 L5 异质性是 schema reactivation 的表型指标

高 L5 heterogeneity 表示 L5 群体不是均匀低水平响应，而是出现了选择性响应子群。

在当前模型中，这些选择性响应子群往往对应：

- reward-related L5RewardRecv cells；
- readout-aligned L5Read cells；
- 被 recurrent schema 共同绑定的 L5 ensemble。

因此，L5 heterogeneity 可以理解为：

> 预训练形成的 L5 schema 在新任务 cue 下被重新激活的程度。

当这个 schema reactivation 更强时，cue 更快获得对 readout 的控制，学习斜率更高。

---

## 5.3 Transfer 的斜率优势来自“继承结构 + 新任务 Hebbian 调整”

Transfer 的学习不是从零开始，而是从一个已有结构出发：

```text
pretraining schema
+ cue modality overlap
+ new cue Hebbian tuning
= faster cue-to-readout mapping
```

Naive 则需要同时建立：

```text
cue → L2/3 map
L2/3 → L5RewardRecv recurrent coupling
L2/3 → L5Read recurrent coupling
reward → L5RewardRecv map
reward/readout/cue joint schema
```

因此 Naive 在相同试次数和相同 learning rate 下更慢。

---

# 6. 图 2：L2/3 响应异质性与学习速度相关性的来源

图 2 的横轴是：

```matlab
Summary.CorrMouse.MeanH23
```

纵轴是：

```matlab
Summary.CorrMouse.Slope
```

相关性计算为：

```matlab
[rhoL23, pL23] = corr(Summary.CorrMouse.MeanH23, Summary.CorrMouse.Slope, ...
    'Type', 'Spearman', 'Rows', 'complete');
```

因此该图使用所有条件、所有 mice 的 L2/3 process heterogeneity 与学习斜率做 Spearman 相关。

---

## 6.1 L2/3 异质性首先来自 cue afferent map 的随机投影

L2/3 activity 由 `CueIn` 通过 `W_CueInputToL23` 产生：

```matlab
preL23_cue = Mouse.W_CueInputToL23 * cueInput_cue + Params.NoiseCue * randn(Params.NL23, 1);
rL23_cue = iRunArea(preL23_cue, 'l23', Mouse, Params);
```

由于 `W_CueInputToL23` 随机初始化，不同 L2/3 cells 对 cue pattern 的投影强度不同。

因此初始 L2/3 cue response 已经具有细胞间差异。

---

## 6.2 Hebbian afferent learning 会放大 cue-aligned L2/3 cells

learning phase 更新：

```matlab
Mouse.W_CueInputToL23 = iHebbAfferent(Mouse.W_CueInputToL23, rL23_L, cueInput_L, eta, Params.AfferentWCap);
```

这意味着对 cue 更强响应的 L2/3 cells 会进一步强化其 cue afferent weights。

因此，L2/3 异质性反映了：

> 该 mouse 是否形成了一个清晰、稀疏、可重复的 cue-responsive L2/3 ensemble。

---

## 6.3 清晰的 L2/3 ensemble 更容易驱动 L5 readout learning

recurrent Hebbian 更新使用完整 internal activity：

```matlab
internalActivity_L = [rL23_L; rL5RewardRecv_L; rL5Read_L];
Mouse.W_L23L5ToL23L5 = iHebbNoSelf(Mouse.W_L23L5ToL23L5, internalActivity_L, eta, Params.WCap);
```

如果 L2/3 cue representation 更有结构，即一部分 cells 稳定强响应、另一部分弱响应，那么 Hebbian 更新会更有效地把这个 L2/3 ensemble 绑定到：

- reward-driven L5RewardRecv ensemble；
- readout-driven L5Read target pattern。

这样，后续 decision phase 中：

```text
cue → L2/3 ensemble → recurrent L5Read activation → hit
```

会更快形成。

所以，在模型中 L2/3 heterogeneity 与 learning slope 的正相关来源是：

```text
更清晰的 cue-responsive L2/3 ensemble
→ 更强、更稳定的 L2/3-to-L5 Hebbian binding
→ 更快的 cue-to-readout pathway formation
→ 更大学习斜率
```

---

## 6.4 在 Transfer 中，L2/3 异质性还反映新 cue 与旧 schema 的对接程度

Transfer mice 已经有预训练 schema。新 cue 若能激活与 pretraining cue 相近的 L2/3 subspace，就更容易接入既有 recurrent matrix。

因此 Transfer 中较高的 L2/3 heterogeneity 往往意味着：

```text
新 cue 激活了更明确的 L2/3 ensemble
且该 ensemble 更容易通过 recurrent schema 连接到 L5 readout
```

这会进一步加快学习。

---

# 7. Naive、Transfer、THOff 三组关系

## 7.1 Naive

Naive 没有预训练 schema。

其学习需要从随机初始状态开始建立：

```text
cue → L2/3 → L5RewardRecv/L5Read → hit
```

因此初期 L5 response 较弱，L5 heterogeneity 较低，学习速度较慢。

---

## 7.2 Transfer

Transfer 经过完整预训练，且新旧 cue 有重叠。

因此新任务初期即可部分重用：

```text
pretrained L2/3-L5 recurrent schema
reward-to-L5RewardRecv map
L5Read target coupling
```

所以 Transfer 表现为：

```text
更高初始 hit rate
更高 L5 heterogeneity
更快 learning slope
```

---

## 7.3 THOff

在当前模型中，`THOff` 表示新任务阶段 reward cells 沉默：

```matlab
Cond.RewardInputLevel = 0.00;
```

即新任务阶段 reward cells 沉默。

预训练阶段 reward input 保持完整：

```matlab
pretrainCond.RewardInputLevel = 1.00;
```

因此 THOff 在新任务开始时继承预训练 schema，但在新任务 learning phase 中不获得 reward-driven L5RewardRecv teaching input。

如果 `THOff` 的学习速度没有显著下降，主要原因是当前模型包含直接 L5Read teaching signal：

```matlab
preL5Read_L = Params.ReadInputGain * Mouse.L5ReadoutPattern + Params.NoiseRead * randn(Params.NL5Read, 1);
```

所以即使 reward cells 沉默，模型也可通过：

```text
cue-responsive L2/3 activity + direct L5Read teaching
→ recurrent Hebbian update
→ cue-to-readout learning
```

继续学习。

---

# 8. 一句话总结

当前模型中，响应异质性来自随机初始投影在 Hebbian 学习中的选择性放大；Transfer 由于预训练形成了可复用的 L2/3-L5-reward-readout schema，新 cue 又与旧 cue 部分重叠，因此能在 L5 中更强地重激活 reward/readout-related ensemble，表现为更高 L5 异质性和更快学习斜率。图 2 中 L2/3 异质性与学习速度的相关性，则来源于更清晰的 cue-responsive L2/3 ensemble 更容易通过 recurrent Hebbian 更新绑定到 L5 readout，从而更快形成 cue-to-action 映射。
