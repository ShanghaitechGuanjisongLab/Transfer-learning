是该兴奋得不够兴奋，还是该沉默的不够沉默？如果是不够兴奋，是增长太慢还是遇到瓶颈？是多数鼠普遍如此，还是少数鼠拉低均值？是因为权重到上限了吗？
MATLAB：多鼠/多条件模型诊断和验证要优先并行运算（如 `parfor`/并行池），不要默认串行跑样本循环。要善于使用dbstop等方法进行单步断点调试，而不是只考虑输出诊断信息到命令行。
如果诊断时先遇到了其它错误，就直接转向诊断新错误，不要执着于我说的错误